#!/usr/bin/env bash
# Hermetic unit tests for user_signals — no network calls.
# Run from repo root: bin/test
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT"

PYTHON_BIN="${PYTHON:-python3}"
"$PYTHON_BIN" -m unittest discover -s user_signals/tests -p "test_*.py" -v

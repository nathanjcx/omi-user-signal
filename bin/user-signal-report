#!/usr/bin/env bash
set -euo pipefail

if ! ROOT="$(git rev-parse --show-toplevel 2>/dev/null)"; then
  ROOT="$(cd "$(dirname "$0")/.." && pwd)"
fi
cd "$ROOT"

PYTHON_BIN="${PYTHON:-python3}"
export PYTHONPATH="$ROOT:${PYTHONPATH:-}"
exec "$PYTHON_BIN" -m user_signals.cli "$@"
